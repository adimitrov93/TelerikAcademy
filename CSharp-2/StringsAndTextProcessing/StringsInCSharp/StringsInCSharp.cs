//Describe the strings in C#. What is typical for the string data type? Describe the most important methods of the String class.

using System;

class StringsInCSharp
{
    static void Main()
    {
        /*
         * String Class represents text as a series of Unicode characters. In fact it nothing more than array of characters.
         * The most important thing about the string in C# is that, it is immutable (it can be changed). Important methods in the
         * string class are IndexOf(), LastIndexOf(), Split(), the static method Format(), ToLower(), ToUpper, etc.
         */
    }
}